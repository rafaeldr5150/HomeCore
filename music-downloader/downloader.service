[Unit]
Description=Music Downloader
After=network.target

[Service]
ExecStart=uvicorn main:app --host 0.0.0.0 --port 8888
WorkingDirectory=/opt/downloader
Restart=always
User=root

[Install]
WantedBy=multi-user.target
